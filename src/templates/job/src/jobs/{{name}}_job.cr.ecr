class <%= @name.capitalize %>Job < BaseJob
  def perform(<%= @fields.map{|f| "#{f.name} : #{f.cr_type}"}.join(", ") %>)
  end
end

# To execute: <%= @name.capitalize %>Job.async.perform(<%= @fields.map(&.name).join(", ") %>)
